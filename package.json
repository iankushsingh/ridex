{
  "name": "ridex-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "rydex"
  ],
  "scripts": {
    "dev": "npm run dev --prefix rydex",
    "build": "npm run build --prefix rydex",
    "preview": "cd rydex && npm run preview",
    "deploy": "cd rydex && npm run deploy"
  }
}
