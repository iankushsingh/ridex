{
  "name": "ridex-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "rydex"
  ],
  "scripts": {
    "dev": "npm run dev --prefix rydex",
    "build": "npm run build --prefix rydex",
    "start": "npm run start --prefix rydex"
  }
}
