{
  "name": "ridex-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix rydex",
    "build": "npm run build --prefix rydex",
    "preview": "npm run preview --prefix rydex",
    "deploy": "npm run deploy --prefix rydex"
  }
}
